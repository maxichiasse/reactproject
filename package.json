{
  "name": "githelper-fullstack",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm run build:backend",
    "start:full": "npm run build && cd backend && npm run start:backend"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0"
  },
  "dependencies": {
    "node-cache": "^5.1.2"
  }
}
